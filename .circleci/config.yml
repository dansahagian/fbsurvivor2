version: 2.1

jobs:
  build:
    docker:
      - image: 'circleci/python:3.8.3'
    steps:
      - checkout
      - run: sudo pip install pipenv
      - run: pipenv install
      - run: echo 'hello world'
